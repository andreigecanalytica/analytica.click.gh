# Agent Rules

## analytica.click.gh Action Reference

- Consumer workflows MUST reference this action as `andreigecanalytica/analytica.click.gh@v2`.
- NEVER use any other ref: not `@main`, not a commit SHA, not another tag.
- NEVER bump or suggest bumping the tag/version. The `v2` tag is the only supported reference and carries both the `pre` and `main` hooks.
