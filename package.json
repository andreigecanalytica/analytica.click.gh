{
  "name": "analytica.click.gh-build",
  "version": "2.0.0",
  "description": "",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "author": " <help@analytica.click> (analytica.click)",
  "license": "ISC",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/andreigecanalytica/analytica.click.gh.git"
  },
  "scripts": {
    "lint": "eslint src",
    "format": "eslint src --fix",
    "build-direct": "rimraf .build && rimraf dist && cross-env NODE_ENV=prod ts-node esbuild",
    "build": "pnpm run lint && pnpm build-direct",
    "start": "ts-node src/direct.ts"
  },
  "dependencies": {
    "@actions/core": "2.0.1",
    "@types/node": "25.0.3",
    "analytica.click": "0.0.585",
    "cross-env": "10.1.0",
    "cross-fetch": "4.1.0",
    "esbuild": "0.27.2",
    "eslint-config-e7npm": "0.1.30",
    "rimraf": "6.1.2",
    "ts-node": "10.9.2",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18",
    "yarn": "use pnpm",
    "npm": "use pnpm",
    "pnpm": ">=8.6"
  },
  "devDependencies": {
    "eslint": "9.39.2"
  }
}